{
  "name": "prototype-review-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute roysoft-review-db --file=src/db/schema.sql",
    "db:init:remote": "wrangler d1 execute roysoft-review-db --remote --file=src/db/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.67.1"
  }
}
